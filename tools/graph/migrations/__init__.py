"""破壊的変更の移行スクリプトを置く場所。

**まだ 1 つもない。** 枠組みは実例が 2〜3 個たまってから設計する。
1 例だけを見て枠組みを作ると、たいてい 2 例目で作り直すことになる。

## 書くとき

テンプレートに破壊的変更（major を上げる変更）を入れるときは、
可能なら移行スクリプトも同じコミットで置く。

ファイル名は `<版>_<内容>.py`。単体で実行できるようにする。

    python -m tools.graph.migrations.v2_0_0_api_to_contract

## 何を書くか

**テンプレート側のファイルは触らない。** `schema.py` も雛形も、
`git merge template/main` が運んでくる。移行スクリプトが直すのは
**プロジェクト側の内容だけ**である。

`40-api` を `40-contracts` に改称したときを例にすると、分担はこうなる。

| 作業 | 誰が |
| --- | --- |
| `schema.py` の語彙、雛形の改名 | マージ |
| ノードの id・type・配置、目次の移動 | **移行スクリプト** |
| 散文中の「40-api」のような記述 | 人間 |

## 何を使うか

`rename` の仕組みをそのまま呼ぶ。id の置換・型の書き換え・ファイル移動・
参照の追随はすでに実装されている。

    from ..loader import load
    from ..rename import rename

## 守ること

- **作業ツリーが汚れていたら中止する。** 失敗しても `git checkout .` で戻せる状態にする
- 何を変えたかを標準出力に出す。黙って直さない
- 直せない部分（散文など）は最後に「手で直すもの」として列挙する
- 移行できる対象が無ければ、その旨を出して正常終了する（二重実行に耐える）
"""
